

import UIKit
import Foundation

public extension UIColor {
    {{#each colors}}
        static let {{name}}: UIColor = UIColor(red: {{red}}, green, {{green}}, blue: {{blue}}, alpha: 1.0)
    {{/each}}
}